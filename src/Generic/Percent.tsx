import React, { ReactElement } from 'react';
import Typography from '@material-ui/core/Typography';

export default function Percent(): ReactElement {
  return (
    <Typography variant="h4" component="span">
      %
    </Typography>
  );
}
